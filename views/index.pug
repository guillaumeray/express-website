extends layout

block content
  h1= title
  p Welcome to #[em OPinion website], a very basic Express website.

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else
    p The library has the following record counts:

    ul
      li #[strong Opinions:] !{data.opinion_count}
      li #[strong Users:] !{data.user_count}
      li #[strong Category:] !{data.category_count}
